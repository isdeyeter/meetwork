---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="TEŞVİK VE HİBE - MeetWork">

  <!-- Hero Section -->
  <section class="py-5 bg-gradient-primary text-white">
    <div class="container">
      <div class="row justify-content-center text-center">
        <div class="col-lg-8">
          <h1 class="display-4 fw-bold mb-4">TEST</h1>
          <p class="lead mb-4">
            deneme sayfası
          </p>
        </div>
      </div>
    </div>
  </section>
  
    </div>
  </div>
</div>

<style>
  .teşvik-section {
    scroll-margin-top: 80px;
  }
  
  .hover-lift {
    transition: all 0.3s ease;
  }
  
  .hover-lift:hover {
    transform: translateY(-5px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.1) !important;
  }
  
  .text-brown {
    color: #8B4513 !important;
  }
  
  .card {
    transition: transform 0.2s ease;
  }
  
  .card:hover {
    transform: translateY(-2px);
  }

  .teşvik-section {
    scroll-margin-top: 80px;
  }
  
  /* Erasmus+ Renk Teması */
  .bg-eu-blue {
    background: linear-gradient(135deg, #003399 0%, #0055a4 100%) !important;
  }
  
  .bg-youth {
    background: linear-gradient(135deg, #ff6b35 0%, #ff8e53 100%) !important;
  }
  
  .bg-education {
    background: linear-gradient(135deg, #00a3a3 0%, #00c9c9 100%) !important;
  }
  
  .bg-teacher {
    background: linear-gradient(135deg, #8a2be2 0%, #9b4de3 100%) !important;
  }
  
  .bg-organization {
    background: linear-gradient(135deg, #d63384 0%, #e2559d 100%) !important;
  }
  
  .btn-outline-eu-blue {
    border-color: #003399;
    color: #003399;
  }
  
  .btn-outline-eu-blue:hover {
    background-color: #003399;
    color: white;
  }
  
  .hover-lift {
    transition: all 0.3s ease;
  }
  
  .hover-lift:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15) !important;
  }
  
  .fırsat-detay {
    border-top: 1px solid #f8f9fa;
    padding-top: 1rem;
    margin-top: 1rem;
  }
  
  .card {
    transition: transform 0.2s ease;
  }
  
  .card:hover {
    transform: translateY(-2px);
  }

    .bg-gradient-primary {
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
    }
    
    .hover-lift {
      transition: all 0.3s ease;
    }
    .hover-lift:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.15);
    }
    
    .teşvik-section {
      scroll-margin-top: 100px;
    }
    
    .card {
      transition: transform 0.2s ease;
    }
    .card:hover {
      transform: translateY(-2px);
    }
  </style>

</BaseLayout>